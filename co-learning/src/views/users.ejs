<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1>Esto son todos los users en DB</h1>
    <% users.forEach(user => { %>
    <div>
      <h2><%= user.name %></h2>
      <p><%= user.country %></p>
      <p>
        Hola, soy de <%= user.city %> y mi número de la suerte es <%=
        user.favoriteNumber %>
      </p>
    </div>
    <% }) %>
  </body>
</html>
